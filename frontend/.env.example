# Frontend environment variables
# Copy this file to .env.local and fill in values for your environment

# API base URL — override when serving frontend separately from the backend
# Leave empty (or unset) to use the default /api proxy (Vite dev) or relative path (production)
VITE_API_URL=
